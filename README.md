# dragons-table-public

Public asset host for The Dragon's Table.

The nightly dev runner captures screenshots of fixes/features and stores them under `images/` so they can be embedded inline in issues on the (private) main repo. These are ephemeral dev UI captures.